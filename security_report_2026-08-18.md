# تقرير أمني: QuizSpace

**تاريخ الفحص:** 18 أغسطس 2026  
**النطاق:** واجهة QuizSpace، إجراءات Supabase، قواعد RLS، متجر المكافآت، ومسارات المؤسسة والاشتراكات.  
**المنهج:** مراجعة تعريفات الإجراءات ذات الصلاحيات المرتفعة، فحص مستشار الأمان في Supabase، تدقيق مسارات الاستحقاق التي تم تعديلها، ومسح المصدر بحثاً عن أسرار تشغيلية مكشوفة.

## الملخص التنفيذي

> **لم يثبت في هذا الفحص وجود ثغرة حرجة قابلة للاستغلال مباشرة.**

لم يعرض فحص Supabase أي نتيجة من مستوى `ERROR`. ظهرت **95 ملاحظة تحذيرية** مرتبطة بدوال `SECURITY DEFINER` المتاحة لدور المستخدم المصادق. هذا لا يساوي تلقائياً وجود اختراق: بعض هذه الدوال مطلوبة لمسارات مثل شراء المكافآت وإدارة المؤسسة، وتحتوي حراس صلاحية داخلية. لكنه يعني أن سطح هجوم الإجراءات كبير ويحتاج برنامج تقليص ومراجعة منهجي.

| التصنيف | النتيجة | الأولوية |
|---|---|---|
| ثغرات حرجة مؤكدة | لا توجد نتيجة مؤكدة في نطاق الفحص الحالي | لا يوجد إجراء طارئ مثبت |
| خطأ صلاحيات المؤسسات | كان يمنع مالك Diamond من قراءة مؤسسته؛ تم إصلاحه بمنح قراءة `authenticated` مع بقاء RLS مقيداً بالمالك/العضو | مغلق |
| منطق حفظ الغلاف | كانت مزامنة تسجيل الدخول تستطيع مسح إعدادات الغلاف النصية؛ تم إصلاح المسار وإضافة استعادة احتياطية | مغلق بعد النشر |
| دوال SECURITY DEFINER | 95 تحذيراً؛ يتطلب كل إجراء غير ضروري سحب صلاحية التنفيذ أو إضافة حارس داخلي صريح | مرتفع من ناحية الصيانة |
| دالة تهيئة المستخدم | `handle_new_user()` دالة trigger ولا تحتاج أن تكون متاحة عبر RPC للحسابات المصادقة | متوسط |
| أسرار داخل المصدر | لم يظهر مفتاح خدمة أو مفتاح LLM حقيقي ثابت داخل `src/` أو `worker/` أو `supabase/`؛ النتائج كانت أسماء متغيرات بيئة أو بيانات اختبار | مغلق ضمن النطاق |

## الملاحظات المؤكدة

### 1. صلاحية تنفيذ دالة التهيئة `handle_new_user`

**الحالة:** متوسطة، لا توجد دلالة على استغلال قائم.

الدالة `public.handle_new_user()` هي trigger ينشئ صف المستخدم بعد التسجيل. تعريفها يعتمد على `NEW`، لذلك وظيفتها الطبيعية أن تُستدعى من trigger لا من واجهة RPC. مستشار Supabase يضعها ضمن الدوال ذات `SECURITY DEFINER` المتاحة للحسابات المصادقة. هذا يزيد سطح الهجوم دون حاجة تشغيلية واضحة.

**المعالجة المقترحة:** سحب `EXECUTE` من `anon` و`authenticated` لهذه الدالة والإبقاء على trigger العامل في الخلفية فقط. لا ينبغي تحويل الدالة إلى `SECURITY INVOKER` لأنها تحتاج صلاحية إنشاء صف المستخدم من trigger.

### 2. مساحة إجراءات `SECURITY DEFINER` كبيرة

**الحالة:** خطر هندسي مرتفع، وليس ثغرة حرجة مثبتة لكل دالة.

الإجراءات ذات `SECURITY DEFINER` تتجاوز صلاحيات المنفذ المباشرة، ولذلك يجب أن تفرض التحقق من الهوية والصلاحية داخل جسم الإجراء نفسه، مع `search_path` ثابت. العديد من إجراءات QuizSpace تفعل ذلك فعلاً، ومنها إجراءات اعتماد الاشتراك وإدارة المؤسسة ومتجر المكافآت الجديدة. ومع ذلك، وجود 95 تحذيراً يعني أنه لا يصح افتراض سلامة كل الدوال القديمة دون فحص منفصل.[1]

**المعالجة المقترحة:** إعداد قائمة سماح للدوال المطلوب تنفيذها من العميل، ومراجعة كل دالة أخرى وفق ترتيب: عمليات النقاط والرصيد، الاستحقاقات والاشتراكات، بيانات المؤسسة، ثم الإحصاءات العامة. يجب سحب `EXECUTE` من كل دالة داخلية أو trigger أو helper لا تحتاجها الواجهة.

### 3. الحماية الجديدة لمتجر الإطارات

**الحالة:** محمية في الإصلاح الحالي.

تمت إضافة عمليتين فقط لإدارة الكتالوج: حفظ إطار وتغيير ظهوره. كلتاهما تعملان عبر `SECURITY DEFINER` وتتحققان من `auth.uid()` ومن قيمة `users.is_admin = true` قبل أي كتابة. كما تتحققان من معرف المنتج، وخطة الحد الأدنى، والأسعار غير السالبة، وتمنعان تكرار صورة إطار نشط. الكتابة المباشرة من العميل على جدول الكتالوج غير مطلوبة.[2]

## ما تم التحقق منه

| المجال | نتيجة الفحص |
|---|---|
| RLS للمؤسسات | القراءة مقيدة بالمالك/العضو، وتصريح دور `authenticated` اللازم لتفعيل RLS أصبح موجوداً. |
| اعتماد التجارب والاشتراكات | الإجراء يتحقق من صلاحية السوبر أدمن، وحالة الطلب، ويمنع استبدال باقة مدفوعة بتجربة. |
| متجر الإطارات | الشراء لا يقرأ إلا عناصر فعالة، وتفعيل الإطار يتطلب ملكيته وإطاراً فعالاً. |
| مفاتيح حساسة | لم يعثر المسح المحدد على قيم سرية حقيقية في المصدر؛ استخدام متغيرات بيئة في Worker هو النمط الصحيح. |
| أخطاء حرجة من مستشار Supabase | لا توجد نتائج `ERROR` في وقت الفحص. |

## خطة المعالجة الموصى بها

| الترتيب | الإجراء | الأثر |
|---|---|---|
| 1 | سحب تنفيذ `handle_new_user()` من أدوار العميل | تقليل سطح RPC غير الضروري فوراً |
| 2 | تدقيق قائمة دوال `SECURITY DEFINER` وإنشاء allowlist | منع بقاء دوال قديمة أو مساعدة متاحة بلا مبرر |
| 3 | إضافة اختبار آلي لكل RPC حساس: مستخدم عادي، مالك، مدير مؤسسة، وسوبر أدمن | كشف أي تجاوز صلاحيات قبل النشر |
| 4 | متابعة مستشار Supabase بعد كل ترحيل | رصد RLS أو صلاحيات مفقودة مبكراً |
| 5 | استكمال ميزة سجلات أمنية إدارية للتغييرات الحساسة في الكتالوج والمؤسسة | دعم التحقيق والمراجعة التشغيلية |

## الحدود

هذا التقرير **ليس اختبار اختراق خارجي شامل** ولا يزعم غياب جميع المخاطر الممكنة. لا يشمل هندسة اجتماعية، ضغطاً على البنية، أو استغلالات متصفح متقدمة. التصنيف هنا يقتصر على الثغرات المثبتة من المصدر وقاعدة البيانات والحالة التشغيلية المتاحة.

## المراجع

[1]: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable "Supabase: SECURITY DEFINER functions executable by authenticated users"
[2]: https://supabase.com/docs/guides/database/functions "Supabase Database Functions and security guidance"
