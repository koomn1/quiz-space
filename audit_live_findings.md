# Live Audit Findings — 2026-08-14

- On `https://koomn1.github.io/quiz-space/#/dashboard/classrooms`, a valid YouTube lesson for classroom `V77ZT0` initially failed to save even though the authenticated user ID matched the classroom owner ID.
- Root cause: the `classroom_lesson_notification` trigger inserted a notification with `type = 'lesson'`, while `public.notifications` accepted only `info`, `community`, `system`, and `promotion`.
- The issue was fixed live through the migration `notification_lesson_and_platform_settings_access`; retrying the same lesson saved it successfully and the test lesson was then deleted successfully.
- PostgreSQL logs also showed repeated `permission denied for table platform_settings` errors on the public read path. The same migration granted read-only `SELECT` to `anon` and `authenticated`; RLS and the protected update RPC continue to govern writes.
- On `https://koomn1.github.io/quiz-space/#/dashboard/motivation`, claiming the completed weekly task `weekly_complete_three` failed with the visible message `تعذر جمع مكافأة المهمة الأسبوعية الآن`.
- Root cause candidate confirmed in migration source: `claim_weekly_task` inserts a notification with `type = 'weekly_task'`, which is not in the existing `notifications_type_check` constraint. The constraint must include both `lesson` and `weekly_task`.
- On the point-store page, `حزمة نقاط صغيرة` and `صندوق نقاط كبير` were rendered as `0 EGP` while their configured rewards were also zero. The live database now sets them to `25 EGP → 500 points` and `120 EGP → 2,500 points` respectively. The frontend now derives a single explicit payment mode and does not present an unpriced item as purchasable.
- The GitHub Pages run for commit `4047f54` completed successfully. An earlier cached document referenced a retired hashed bundle and returned the SPA 404 fallback when fetched again. Loading a fresh document URL with a pre-hash cache key retrieved the deployed code, and the monthly passes then displayed their correct `50,000` and `100,000` point prices. No active deploy artifact loss remained after the fresh load.
- On `https://koomn1.github.io/quiz-space/#/dashboard/profile`, copying the username and profile link succeeded. Re-selecting the active diamond frame and a built-in cover, then saving, showed a success message; a full page reload retained the selected library-style cover and the diamond frame.
- On `https://koomn1.github.io/quiz-space/#/dashboard/create`, all four creation methods were listed. The AI method accepted the test topic `النظام الشمسي`, and the minimum three-question quantity was selected. The request completed, produced exactly three editable questions, and returned clear batch-progress feedback. Its essay answer, however, included the Korean token `약` and the Spanish word `proceso`; the new prompt constraint and client-side validation now reject such a result and continue to a fallback provider for Arabic-topic generation.
- Direct diagnostic calls to `https://quiz-space-ai.yo01009950871.workers.dev/api/ai/generate` showed that the Groq path can return Arabic text with foreign-script contamination, while OpenRouter and DeepSeek returned HTTP 502 in the same period. The first live sanitation retry still left the uppercase English word `Contains` and Thai characters in an essay field because the initial filter covered only lowercase Latin. Commit `ce3974d` expands the protection to all Latin characters and the foreign scripts observed in the audit; its final live recheck is in progress.
- The final live recheck on commit `ce3974d` generated a three-question draft about `الغلاف الجوي`. The draft contained no Latin, Korean, Chinese, Cyrillic, or Thai characters in any title, question, option, answer, or explanation. The generator still needs a separate pedagogical-quality refinement pass for occasional awkward Arabic phrasing, but the foreign-language leakage defect is resolved.
- The deployed store now presents `تفعيل ذهبي (شهر)` as `عضوية ذهبية لمدة شهر` with an explicit `50,000 نقطة` purchase cost, and `تفعيل ماسي (شهر)` as `عضوية ماسية لمدة شهر` with an explicit `100,000 نقطة` purchase cost. The misleading zero reward label no longer appears.
- After deployment of commit `7258ae3`, a fresh authenticated page load for the same account did not reopen the onboarding tour. The account row was already marked `onboarded = true`; the repair waits for that durable state before deciding whether the tour may be shown.
- On the unpublished `الغلاف الجوي` draft, duplicate filtering correctly reported no duplicates. Adding one empty manual question increased the working draft to four questions, and deleting that same question restored it to three; no quiz was saved or published. The final publication controls remain deferred because they create public content.
- On the registered `#/dashboard/aichat` route, Cosmo loaded the existing conversation history. A short internal question, `عرّف الغلاف الجوي في جملة واحدة.`, was saved and received a coherent Arabic reply. The earlier `#/dashboard/cosmo` URL is not a registered route and correctly should not be used as an audit target.
- An owned zero-priced frame, including the Diamond Comet frame, was incorrectly rendered with a disabled `استخدام` control because the store reused purchase availability for the activation path. Commit `8ba6fb3` separates action availability from purchase availability. Live verification activated Diamond Comet, updated the button to `مفعل`, and retained it after a fresh page load.
- The Lucky Wheel originally animated before asking the server whether the user had already spun, then showed an English rejection while leaving the control reusable. Commit `fe2b195` claims before animating and normalizes the completed-state feedback. With a real claim already recorded for the day, the live control immediately became disabled with `تم اللعب اليوم` and the Arabic message `لقد أدرت العجلة اليوم. عد غداً.`; the wheel did not animate.
- Learning-streak reads and writes were already bound to `auth.uid()` at the RPC layer, but the frontend still derived a profile streak from completion counts and showed the daily-gift streak from a legacy reward-balance mirror. Migration `20260833_learning_streak_user_isolation.sql` hardens the per-user row and its RLS policy, makes first-time streak creation concurrency-safe, and keeps the legacy mirror synchronized. Commits `1c1539a` and `7f9f064` make all private displays consume the canonical user-scoped status. A rollback-based two-account write test observed separate streaks (3 versus 1) and left no test data; the final live page displayed 3 days in both the momentum card and daily-gift section.
- Weekly-task collection failed because the production `notifications_type_check` constraint allowed `lesson` but not `weekly_task`; the notification insert rolled back the reward transaction. Migration `20260834_weekly_task_notification_type.sql` adds the missing type. Live collection of `weekly_complete_three` succeeded with +75 points and +10 coins; the interface marked it `تم الجمع`, the ledger recorded `weekly_task:2026-08-10:weekly_complete_three`, and the matching `weekly_task` notification was saved.
