# Profile assets verification — 2026-08-15

## Local preview

The local Vite preview loaded successfully at the `/quiz-space/` base path after the initial page settled. The Arabic RTL homepage rendered with the existing navigation, authentication controls, hero section, and quiz catalog. No JavaScript errors were emitted in the browser console during the smoke check.

## Database catalog verification

Supabase project `eqfrxrsstatryaetgerw` was used. The catalog now contains distinct generated assets for `frame_diamond_comet`, `frame_diamond_crown`, `frame_ramadan_lantern`, and `frame_back_to_school`, plus the two free frames. `offer_vip_combo` remains an active store offer but is marked featured so it does not appear as a duplicate standalone frame in the catalog. A partial unique index protects active standalone frame image paths.

## Design decisions

The profile picker uses eight curated avatar presets, lazy loading, descriptive Arabic/English labels, and 44px minimum touch targets. Frame previews use `object-contain` and a smaller overlay scale so transparent rings sit around the circular photo without cropping. The old 12 cartoon avatar families were removed from both the picker and public asset folder; default-avatar selection now reads from the new catalog.

## Mobile capture

A 375×812 Chromium capture loaded the Arabic mobile shell without horizontal overflow or clipped controls. The settled capture still showed the app's resource-progress screen at 84%, indicating the local preview was still loading its existing heavy modules within the capture budget; this is not caused by the profile asset changes, and the normal browser preview later settled into the homepage.

## Published smoke check

The published QuizSpace homepage loaded after its synchronization screen completed and rendered the RTL navigation, catalog, and owned-quiz labels. The current browser session displayed the visitor state and the “التسجيل / الدخول” control, so an authenticated profile/store smoke test was not performed; no login or sensitive input was attempted during this pass.

## Authenticated published smoke check

After the user signed in, the live GitHub Pages bundle opened the profile editor for the authenticated account. The live picker still rendered retired boy-cartoon-1..6 and girl-cartoon-1..6 entries, plus old avatar and frame paths. This confirms the local catalog and fallback fixes had not yet been pushed or published at the time of inspection; it is a deployment-state finding rather than a local-code regression.

The deterministic-asset commit was then pushed to main. GitHub Actions Build & Deploy completed successfully, including frontend build, Pages deployment, and AI Worker deployment. A cache-busted authenticated reload reached the new bundle and was still completing its account synchronization screen at the last capture; the final profile-picker verification follows after the page settles.

On the first settled attempt the app showed its built-in “تعذر فتح هذه الصفحة” refresh recovery state, which is the expected stale-bundle guard after a Pages update. Clicking its reload control started a fresh authenticated synchronization at 20%; no account data was changed. A subsequent wait returned to the same recovery page, and the browser console had no captured error output. Therefore the post-deploy authenticated profile-picker flow remains unverified in this browser session despite successful CI deployment. Resource inspection showed the new main bundle, UserProfile chunk, and all split vendor chunks returning HTTP 200; the page is controlled by the project service worker. This narrows the remaining browser blocker to stale service-worker/runtime state or an uncaptured route-import error, not a missing deterministic profile asset. The service worker was unregistered and one QuizSpace cache entry was deleted from the test browser, but the same recovery page remained after reload. The remaining check should isolate the homepage from the direct profile deep link and inspect the published lazy-import behavior. Direct dynamic import of the published UserProfile chunk succeeded with a default function export, so the chunk itself is valid. A cache-busted homepage navigation was then started in the authenticated session to isolate whether the failure is route-specific. The homepage settled successfully with the authenticated quiz catalog and no recovery state. A console hash-navigation attempt was rejected before execution due to expression syntax and did not change the page; the profile route will be reopened through normal navigation. The homepage remained healthy, but the direct profile deep link again reached the recovery page after settling. This confirms the remaining blocker is route-specific in the published session, not a global frontend outage. A fresh debug-home navigation also settled successfully with the authenticated catalog, confirming the new main bundle remains healthy before internal profile navigation. A temporary window-level error and unhandled-rejection collector was installed successfully; the first console syntax attempt was rejected without side effects. Internal hash navigation from the healthy homepage reproduced the recovery page, but the temporary window error/unhandled-rejection collector remained empty. The error is therefore caught by the React boundary without bubbling to window, or the boundary is being triggered by a synchronous component render exception. The alternate query-based profile route (`tab=profile&profileId=...`) reproduced the same recovery page, so the issue is not limited to hash parsing. The diagnostic deployment completed successfully, but the cache-busted direct profile route still reaches recovery after synchronization; the next capture reads the new boundary log to identify the synchronous render failure. After the subsequent badge-metadata guard deployment, the same authenticated profile route no longer showed recovery and advanced into the profile loading UI, indicating the defensive normalization addressed the first route crash. After one additional wait, however, the page returned to the generic recovery state; the browser console still exposed no stack trace. This means a second synchronous profile-render path remains, so the next hardening step will normalize profile decoration data at the DB-mapping boundary as well as in the visual components. The mapping boundary now applies the same safe defaults before returning UserStats; the focused regression suite passes (3 tests) and TypeScript reports no errors. The subsequent production reload still reached recovery after synchronization, so normalization alone did not close the remaining route failure; the actual profile component render/data path still needs isolation before claiming authenticated persistence verification.


Root cause isolated on 2026-08-15: the persisted boundary payload contained minified React error #310, with the component stack pointing to UserProfile. `handleProfileImageError` was declared with `useCallback` after the `isLoading` early return, so the hook order changed when profile data finished loading. The callback is now declared before the guard; the temporary sessionStorage diagnostic was removed; and `src/pages/UserProfile.hooks.test.ts` protects the ordering. The full suite passed with 27 test files and 91 tests, followed by a clean TypeScript check.
