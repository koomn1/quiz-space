# Avatar and extraction root-cause investigation

Date: 2026-08-26

The user screenshot uses `z-space-app.pages.dev`, which does not resolve from the browser environment (`ERR_NAME_NOT_RESOLVED`). The canonical app is `https://quiz-space-app.pages.dev/`.

The canonical app authenticated successfully as Youssef Badawy after a fresh cache-busted navigation. Its dashboard rendered normally and exposed the profile image path in the rendered content as `/quiz-space/clean-assets-replacement/avatar-boy-music-walker-v2.webp`.

The avatar path is generated by `profileAssetUrl()` from `src/constants/profileAssets.ts`, while Sidebar receives `photoUrl={userPhoto}` from App and AIChat receives `userPhoto={userPhoto}`. Sidebar and MessageRow currently render raw `img` elements with no shared fallback or URL normalization. AIChat's assistant avatar is a separate fixed Cosmo asset.

The next diagnostic step is to inspect the live DOM image URLs/status/natural dimensions and compare the profile stats/auth metadata path, then repair the shared avatar resolution and the extraction JSON failure separately.

## Conclusive DOM evidence

On the canonical authenticated dashboard before the new patch, the profile avatar image had `src` path `/quiz-space/clean-assets-replacement/avatar-boy-music-walker-v2.webp`, `complete: true`, and `naturalWidth: 0`. The same page logo loaded successfully. This proves the avatar failure was a broken legacy base path, not a layout or image-quality problem. The new `normalizeUserPhoto()` path resolver maps this exact legacy path to `/clean-assets-replacement/avatar-boy-music-walker-v2.webp`, and ProfileAvatar also provides an initial fallback if any future image request fails.

The screenshot's `z-space-app.pages.dev` host itself returned DNS `ERR_NAME_NOT_RESOLVED`; it cannot receive the new deployment. The user must open `https://quiz-space-app.pages.dev/`.
