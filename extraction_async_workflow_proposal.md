# مقترح تسريع استخراج الملفات وتوليد الاختبارات

## المشكلة الحالية

المسار الحالي يرسل الملف كـBase64 إلى العامل ثم ينتظر داخل نفس الطلب حتى يكتمل استخراج PDF أو Word أو Excel، وتقسيم المحتوى واستدعاءات النموذج. في الملفات الطويلة، ترتبط شاشة إنشاء الاختبار بزمن المعالجة كله. البث الحالي يحسن التقدم لملفات PDF فقط، لكن Word وExcel ومعالجة الملفات الممسوحة أو الطويلة تظل مرتبطة بطلب مباشر طويل.

## النتيجة المطلوبة

عند رفع الملف، يحصل المستخدم فورًا على رقم مهمة وحالة مفهومة مثل «تم الرفع»، «استخراج النص»، «تحليل الأسئلة»، «مراجعة النتيجة». تستمر المعالجة في الخلفية حتى لو أُغلق الحوار، ثم يظهر الاختبار الجاهز أو خطأ قابل لإعادة المحاولة. لا يحمل العميل الملف إلى النموذج أكثر من مرة، ولا تمر مفاتيح المزود أو بيانات الاعتماد إلى المتصفح.

## مقارنة الخيارات

| النهج | كيف يعمل | المزايا | القيود | التعقيد |
|---|---|---|---|---|
| خدمة خلفية مدمجة بالموقع | يرفع العميل الملف مرة واحدة إلى تخزين خاص، ثم ينشئ مهمة في قاعدة البيانات. عامل آمن يستخرج النص ويولّد الأسئلة ويحفظ النتيجة، بينما الواجهة تتابع الحالة. | أسرع تجربة مستخدم، تحكم كامل في الصلاحيات، لا توجد نقلة إضافية للملف، ويعيد استخدام تقدّم البث الحالي. | يحتاج جدول مهام وتخزين ملفات ومسار عامل موثوق. | متوسط |
| سير عمل n8n | يرسل الموقع Webhook بمهمة وURL موقّع للملف؛ ينفذ n8n الاستخراج واستدعاء النموذج ثم يعيد النتيجة الموقّعة إلى الموقع. | واجهة مرئية للخطوات وإعادة المحاولة، ومرن لو أضيفت لاحقًا إشعارات أو Google Drive أو البريد. | ليس أسرع تلقائيًا؛ يضيف قفزة شبكة وخدمة وبيانات اعتماد جديدة. يحتاج n8n مستضافًا باستمرار وحماية Webhook. | متوسط إلى مرتفع |
| تحسين سريع للمسار الحالي | يبقى الطلب المباشر، مع أولوية parser محلي، تقسيم ذكي حسب السؤال، وبث بدء فوري لكل أنواع الملفات. | أقل تغيير وأسرع إطلاق. | لا يحل استئناف المهمة أو معالجة ملفات طويلة بعد إغلاق الصفحة. | منخفض |

## التصميم المقترح للمسار المدمج

1. **رفع آمن مرة واحدة:** يحصل العميل على رابط رفع موقّع ويرفع الملف إلى تخزين خاص بدل تضمينه في عدة طلبات Base64.
2. **إنشاء مهمة:** ينشئ الموقع صفًا في `extraction_jobs` يحتوي `id`، مالك المهمة، رابط التخزين، نوع الملف، وضع الاستخراج، الحالة، النسبة، وعدد المحاولات.
3. **عامل المعالجة:** يلتقط المهمة، يجرب parser محليًا أولًا للملفات ذات البنية الواضحة، ثم يستخرج النص ويقسمه حسب حدود الأسئلة لا حسب طول عشوائي، ويستدعي النموذج على دفعات محدودة.
4. **نتيجة منظمة:** تحفظ الأسئلة بعد التحقق وإزالة التكرار، مع زمن التنفيذ والمزود ورسالة خطأ آمنة إن وجدت.
5. **تحديث الواجهة:** تعيد الواجهة استخدام عقد التقدم الحالي `init | progress | complete | error`، لكن عبر قراءة حالة المهمة أو تحديث حي بدل إبقاء اتصال واحد طويلًا مفتوحًا.
6. **إتاحة الاستئناف:** عند فتح الصفحة مجددًا، يعيد العميل جلب المهام غير المكتملة للمستخدم ويعرض متابعة النتيجة أو إعادة المحاولة.

## معايير القبول

- استجابة الواجهة الأولى خلال ثانيتين بعد الرفع، حتى لو استغرق الاستخراج دقائق.
- الملف لا يمر إلى النموذج إلا من العامل الموثوق، ولا تظهر مفاتيح المزود في العميل.
- مهمة واحدة فقط لكل رفع، مع مفتاح منع تكرار وإعادة محاولة محكومة.
- يمكن إعادة فتح مهمة مكتملة أو فاشلة واسترجاع النتيجة دون إعادة معالجة الملف.
- تسجل كل مهمة زمن الرفع واستخراج النص وتوليد الأسئلة لمعرفة الاختناق الحقيقي.

## قرار التنفيذ

تكامل n8n موجود لكنه غير مفعّل حاليًا. لا يوصى بتفعيله لمجرد تسريع المسار، لأن نفس نموذج الذكاء الاصطناعي سيظل هو تكلفة الزمن الأساسية. يصبح n8n مناسبًا عندما تريد واجهة لإدارة خطوات خارجية كثيرة، مثل تلقي ملفات من Google Drive أو إرسال تقارير عبر البريد. أما لتسريع Quiz Space الآن، فالمسار المدمج مع مهمة خلفية وتخزين خاص هو الأساس التقني الأنظف، ويمكن ربط n8n به لاحقًا من دون تغيير واجهة المستخدم.
