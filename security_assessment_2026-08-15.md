# تقييم أمان مصرح به وغير مُدمّر

**التاريخ:** 15 أغسطس 2026  
**النطاق:** عزل بيانات المستخدمين، مسارات النقاط والمكافآت، صلاحيات دوال Supabase ذات `SECURITY DEFINER`، وواجهات RPC المعروضة.  
**المنهج:** اختبارات رفض وصول وقراءات فقط. لم تُعدَّل أرصدة أو اختبارات أو بيانات حسابات أخرى. كانت إعادة RPC لمكافأة مرحلة مكتملة آمنة لأن جميع قيود النقاط والشارات كانت موجودة مسبقًا.

## النتيجة المختصرة

كشف التقييم خللًا مؤكدًا في منع تكرار **مكافآت مراحل الاختبارات العادية** وأُصلح فورًا. كانت الدالة القديمة تتجاهل تكرار قيد السجل، لكنها قد تستمر في تحديث الرصيد وXP لمكافأة المرحلة عندما يعاد استدعاء نفس RPC في لحظة إكمال خامسة أو عاشرة. أصبح تحديث الرصيد وXP الآن مشروطًا بنجاح إدراج قيد المرحلة الفريد.

كما أُزيلت صلاحية الاستدعاء عن دالتين داخليتين لا يجب كشفهما كواجهات RPC: دالة التهدئة الديناميكية ودالة trigger الخاصة بالإطار النشط.

| الاختبار | النتيجة | الدليل الآمن |
|---|---|---|
| طلب منح نقاط بلا تسجيل دخول | ناجح | انتهى برفض `401 / 42501` قبل أي كتابة |
| انتحال معرّف مستخدم آخر في فتحة/إكمال الاختبار اليومي | ناجح | انتهى برفض `400 / P0001` قبل أي تغيير |
| قراءة رصيد حساب مختلف فعليًا | ناجح | استجابة `200` مع **0 صفوف** بسبب RLS |
| إعادة RPC لمكافأة إكمال مملوك بعد الإصلاح | ناجح | `200` مع `points_awarded = 0` وبقاء الرصيد `1,081,541` |
| قيد مكافأة المرحلة العاشرة | ناجح | بقي قيد واحد فقط في السجل |
| دالة التهدئة الداخلية كمستخدم مصادق | ناجح | رفض `403 / 42501` بعد سحب التنفيذ |
| دالة trigger كزائر | ناجح | لم تعد متاحة عبر RPC (`404`) |

## الإصلاحات المطبقة

1. تضيف الهجرة `20260840_harden_reward_idempotency_and_rpc_surface.sql` شرطًا يعتمد على `ROW_COUNT` بعد `ON CONFLICT DO NOTHING` قبل زيادة الرصيد أو XP لمكافأتي المرحلة الخامسة والعاشرة. بذلك يصبح المفتاح الفريد في السجل هو المصدر الوحيد الموثوق للسماح بالمنح.
2. سحبت الهجرة `EXECUTE` من `PUBLIC` و`anon` و`authenticated` عن `check_daily_cooldown` و`prevent_unverified_active_frame_update`. لا تستخدم الواجهة أيًا منهما مباشرة.
3. غطى الاختبار `rewardSecurityHardening.test.ts` وجود الحماية البنيوية وقيود الصلاحيات. اجتازت المجموعة الكاملة **57/57** اختبارًا، كما نجح فحص TypeScript وبناء الإنتاج.

## ملاحظات المتابعة

يظهر مستشار Supabase عددًا كبيرًا من تحذيرات `SECURITY DEFINER` لأنها دوال RPC مقصودة للمستخدمين المصادقين. التحذير وحده ليس ثغرة: ينبغي أن يكون لكل دالة فحص ملكية أو تحقق إداري داخلي كما تم التأكد منه في مسارات النقاط والاختبار اليومي. يوصى بتدقيق الدوال المتبقية على دفعات، مع إعطاء الأولوية للدوال الإدارية ودوال إنشاء أو حذف المحتوى.

> التقييم يثبت مقاومة المسارات المختبرة ولا يمثل اختبار اختراق شاملًا أو بديلًا عن مراجعة خارجية مستقلة قبل توسيع الاستخدام أو معالجة مدفوعات حقيقية.
