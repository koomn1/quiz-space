# تقرير أداء المسارات اليومية والمكافآت

**التاريخ:** 15 أغسطس 2026  
**النطاق:** Supabase الإنتاجي لمسارات الرصيد، سجل النقاط، وRPC إكمال الاختبار اليومي.  
**الضوابط:** استُخدم حساب مصادَق عليه، وقراءات فقط للرصيد، واستدعاء RPC لنتيجة يومية مكتملة مسبقًا يعيد `0` نقطة. لم يُنشأ اختبار جديد، ولم يُمنح رصيد جديد، ولم تُعدَّل بيانات أي مستخدم آخر.

## خلاصة

قاعدة البيانات استجابت بصورة سليمة تحت حمل صغير ومحكوم. جميع طلبات القراءة الـ12 نجحت، كما نجحت استدعاءات RPC الثمانية المتوازية من دون أخطاء أو تكرار للمكافأة. زمن قاعدة البيانات لخطة الرصيد والسجل منخفض، بينما زمن الاستجابة الظاهر في المتصفح أعلى لأنه يتضمن المصادقة والشبكة وطبقة PostgREST.

| المسار | الحمل | النجاح | زمن العميل | زمن قاعدة البيانات | النتيجة |
|---|---:|---:|---:|---:|---|
| قراءة الرصيد القانوني | 12 طلبًا، 3 دفعات × 4 متوازية | 12/12 (200) | p50: 408.1ms، p95: 799.4ms | غير مباشر؛ خطة الرصيد: 0.120ms | مستقر، دون رفض أو فقد بيانات |
| RPC إكمال يومي مكتمل | 8 طلبات متوازية | 8/8 (200) | p50: 610.6ms، p95: 882.3ms | يشمل قفل صف وتحديث حالة الإكمال | كل الطلبات أعادت 0 نقطة |
| سجل المكافآت | قراءة أحدث 12 قيدًا | ناجح | غير مقاس من العميل | 2.366ms | استخدم فهرس `reward_points_ledger_user_created_idx` |
| الرصيد القانوني | قراءة صف المستخدم | ناجح | غير مقاس من العميل | 0.120ms | مسح تسلسلي مبرر حاليًا لصغر الجدول (17 صفًا) |

## سلامة البيانات بعد التزامن

ظل رصيد الحساب عند **1,081,541 نقطة**. يحتوي سجل المكافآت على **قيد واحد** فقط لإكمال الاختبار اليومي المقاس، وبقي التقييم المحفوظ **5 نجوم**. لم تظهر أخطاء PostgreSQL أو PostgREST في نافذة الاختبار من 05:18 إلى 05:21 UTC.

> لا تمثل هذه الأرقام اختبار سعة قصوى للمنصة؛ إنها اختبار صحة واستجابة تحت تزامن محدود وآمن على الإنتاج. رفع الحمل إلى مئات المستخدمين يجب أن يُجرى من بيئة اختبار منفصلة وبحسابات اختبار معزولة.

## ملاحظات وتحسينات موصى بها

1. تأخير RPC المتوازي متوقع جزئيًا لأن الطلبات الثمانية حدثت نفس سجل الإكمال، فتُسلسل تحديثات الصف لحماية عدم تكرار النقاط. هذا سلوك صحيح للمعاملة المالية، وليس عيبًا في التكامل.
2. أظهر مستشار Supabase أن سياسات RLS في `user_reward_balances` و`reward_points_ledger` تتضمن استدعاء `auth.uid()` قابلًا لإعادة التقييم لكل صف. يوصى في تحسين مستقل باستبداله بـ`(select auth.uid())` مع الحفاظ على نفس شرط الملكية، ثم إزالة السياسات المكررة بعد مراجعة التفويض. [مرجع Supabase](https://supabase.com/docs/guides/database/row-level-security#call-functions-with-select)
3. أشار المستشار كذلك إلى مفاتيح خارجية غير مفهرسة في جداول تحديات ومواسم لا تقع على مسار الاختبار اليومي الحالي. يوصى بإضافة فهارس تغطية لها في دفعة أداء منفصلة بعد تأكيد أنماط الاستعلام الفعلية. [مرجع Supabase](https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys)
4. فهرس الرصيد الأساسي موجود، لكن المخطط اختار مسحًا تسلسليًا لأن حجم الجدول صغير جدًا؛ لا توجد فائدة عملية من تغيير ذلك قبل نمو جدول الأرصدة.
