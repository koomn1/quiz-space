# معمارية أمان وتشفير تطبيق Quiz Space لنظام Android

## 1. نظرة عامة
تعتمد نسخة Android لـ Quiz Space على حزم محتوى الويب المحلي المضمّنة بالكامل داخل حزمة الويب (`dist`)، مع عزل أي اعتماد على الخوادم الخارجية غير الموثوقة. الهدف هو توفير تجربة مستخدم سريعة، مستقرة، وخالية من الإعلانات أو الثغرات، مع حماية بيانات المستخدمين، النقاط، الأرصدة، والمحتوى التعليمي.

## 2. طبقات الحماية الرئيسية

### أ. التخزين الآمن للجلسات (Secure Storage)
- يتم تخزين بيانات مصادقة Supabase وجلسات المستخدمين باستخدام `SecureStorage` المعتمد على **Android Keystore** و **EncryptedSharedPreferences**.
- يُمنع تخزين رموز الجلسة (Tokens) أو كلمات المرور في `localStorage` الخاص بالـ WebView العادي، مما يمنع التطبيقات الأخرى أو أدوات الفحص من قراءة الجلسة حتى لو تم الوصول إلى مساحة تخزين التطبيق.

### ب. تشديد إعدادات WebView والنافذة
- **منع لقطات الشاشة (FLAG_SECURE):** يتم تطبيق علامة الأمان برمجياً في `MainActivity` لمنع التقاط الشاشة أو تصويرها أثناء تصفح الاختبارات الحساسة أو الحساب الشخصي.
- **تعطيل التصحيح (Debugging):** وضع الإصدار (Release) يعطل كلياً ميزة فحص الـ WebView عبر الأجهزة الخارجية (`WebContentsDebuggingEnabled = false`).
- **منع الوصول للملفات المحلية:** إيقاف `allowUniversalAccessFromFileURLs` و `allowFileAccessFromFileURLs` لمنع استغلال الثغرات عبر الملفات المحلية.

### ج. أمان شبكة الاتصالات (Network Security)
- منع أي حركة بيانات عبر النص الصريح (`usesCleartextTraffic="false"`).
- تقييد الاتصالات بالشهادات المعتمدة في نظام التشغيل فقط عبر ملف `network_security_config.xml`.
- الاتصال بخوادم Supabase و Cloudflare Worker يتم عبر قنوات HTTPS مشفرة حصراً.

### د. حماية البيانات من النسخ الاحتياطي غير المصرح به
- تم ضبط `allowBackup="false"` لمنع نسخ بيانات التطبيق وقواعد البيانات أثناء النسخ الاحتياطي للنظام.
- تطبيق `data-extraction-rules` و `backup-rules` لاستبعاد مجلدات SharedPreferences وقواعد البيانات المحلية من التصدير لأجهزة أخرى.

### هـ. الحماية البرمجية وضغط الكود (R8 / ProGuard)
- تفعيل R8 بمستوى تحسين مرتفع في نسخة الإصدار لضغط وإزالة الأكواد الزائدة.
- إخفاء أسماء الفئات والدوال الحساسة للحد من الهندسة العكسية.

## 3. التكامل مع Supabase والخدمات الخلفية
- جميع عمليات تحديث النقاط، إدارة المؤسسات، ومتجر المكافآت محمية بـ **Row Level Security (RLS)** و **RPC Functions** داخل Supabase.
- لا توجد مفاتيح صيانة (`service_role`) أو أسرار خاصة بمزوّدي الذكاء الاصطناعي مضمنة داخل حزمة الهاتف المحمول، بل يتم التواصل مع Cloudflare Worker عبر مفتاح العميل العام وبشكل موثق بالمستخدم الحالي فقط.
