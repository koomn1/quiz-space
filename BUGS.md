# Quiz Space — Open Bugs and Verification Log

**تاريخ المراجعة:** 2026-08-08  
**النطاق:** XP، الاختبار اليومي، الكوبونات، مراقبة AI، عرض Cosmo، ومسار النشر.  
**حالة البناء المحلي:** `npm run build` ينجح. توجد تحذيرات حجم حزم JavaScript، لكنها ليست أخطاء بناء.

> هذا الملف يميّز بين ما يمكن إثباته من الكود وما يحتاج اختباراً على مشروع Supabase الإنتاجي. نجاح بناء الواجهة لا يثبت أن ملفات SQL طُبقت على قاعدة البيانات.

## ملخص الحالة

| المعرّف | المشكلة | الحالة الحالية | مستوى الخطورة | طريقة التحقق |
|---|---|---|---|---|
| BUG-001 | XP لا يظهر أو يعود إلى صفر بعد تحديث الصفحة | مفتوح حتى التحقق من تطبيق هجرات XP واستعادة الرصيد | حرجة | فحص `users.xp` ونتيجة RPC بعد حل اختبار حقيقي |
| BUG-002 | التحدي اليومي يعيد الاختبار القديم أو لا يعرض العداد | مفتوح حتى تطبيق أحدث هجرة واختبار إعادة الدخول | حرجة | فحص `daily_quiz_user_slots` بعد الحل وقبل/بعد المهلة |
| BUG-003 | التحدي اليومي لا يُحفظ كاختبار عام | الإصلاح موجود في الكود، لكنه يعتمد على هجرات SQL | عالية | التأكد من عدم وجود سجل يومي جديد في `quizzes` |
| BUG-004 | الكوبون يظهر ناجحاً ثم يختفي بعد Refresh | الإصلاح موجود في الكود، ويتطلب RPC الذري في قاعدة البيانات | حرجة | فحص `users`, `coupon_usages`, و`used_count` بعد الاسترداد |
| BUG-005 | مراقبة AI في لوحة الأدمن لا تعرض السجلات | الإصلاح موجود في الكود، ويتطلب RPC وصلاحية الأدمن | عالية | دخول أدمن وفحص نتيجة `get_ai_performance_logs` |
| BUG-006 | Cosmo يعرض جدول Markdown كنص عادي | تم إصلاح renderer في الكود؛ يحتاج تحققاً بصرياً بعد النشر | متوسطة | إرسال طلب مقارنة والتأكد من ظهور `<table>` فعلياً |
| BUG-007 | حالة النشر قد تختلف عن الكود المحلي | مفتوح كإجراء تحقق | حرجة | مراجعة GitHub Actions ونسخة commit المنشورة |

## BUG-001 — XP لا يُستعاد أو يتصفر

### الوصف

المسار الحالي يعتمد على وجود العمود `users.xp` وعلى تطبيق دوال `submit_quiz_attempt` و`submit_user_daily_quiz_attempt` في Supabase. إذا لم تُطبق الهجرة، أو كان التطبيق المنشور يستدعي تعريف RPC أقدم، فقد يُحفظ completion من دون تحديث XP أو تُعرض قيمة صفرية عند تحميل الملف الشخصي.

### خطوات إعادة الإنتاج

1. سجّل الدخول بحساب لديه completion محفوظ.
2. حل اختباراً جديداً ثم أكمل التقييم.
3. افحص قيمة `users.xp` مباشرة في Supabase.
4. نفّذ Refresh أو سجّل خروجاً ثم دخولاً.
5. قارن القيمة في قاعدة البيانات مع القيمة في صفحة الملف الشخصي.

### النتيجة المتوقعة

تزداد قيمة XP مرة واحدة فقط لكل إكمال، وتظل محفوظة بعد إعادة تحميل الصفحة.

### الدليل البرمجي

- `src/lib/db.ts` — دالة `submitQuizAttempt`.
- `src/components/QuizResolver.tsx` — الحفظ التلقائي والحفظ بعد التقييم.
- `supabase/migrations/20260808_repair_xp_and_daily_timer.sql`.
- `supabase/migrations/20260808_repair_daily_completion_and_restore_xp.sql`.

### الإجراء المطلوب

تطبيق أحدث هجرة `20260808_repair_daily_completion_and_restore_xp.sql`، ثم فحص وجود `users.xp`. إذا كان الرصيد قد صُفّر قبل وجود سجلات صحيحة، يمكن إعادة بنائه فقط من `completions` التي ما زالت موجودة؛ لا يمكن استنتاج عمليات مفقودة من الواجهة وحدها.

## BUG-002 — التحدي اليومي يعيد الاختبار القديم ولا يبدأ العداد

### الوصف

التحدي اليومي خاص بالمستخدم ويُخزّن في `daily_quiz_user_slots`. بعد الحل يجب أن تُحفظ النتيجة، يُضاف XP، يُمسح `quiz_payload`، وتُضبط `next_available_at`. إذا كانت الدالة المنشورة قديمة أو لم تُطبق الهجرة الأخيرة، يبقى payload القديم أو لا تصل حالة العداد إلى الواجهة.

### خطوات إعادة الإنتاج

1. افتح التحدي اليومي وسجّل معرفه.
2. حل الاختبار وأكمل التقييم.
3. أغلق الصفحة وافتح صفحة الاختبارات من جديد.
4. راقب هل يظهر التحدي القديم، وهل يظهر عداد للمهلة.
5. افحص صف المستخدم في `daily_quiz_user_slots`.

### النتيجة المتوقعة

بعد الحل يجب أن تكون حالة الصف تقريباً كالتالي:

| الحقل | القيمة المتوقعة |
|---|---|
| `quiz_payload` | `NULL` |
| `quiz_id` | `NULL` |
| `answered_at` | وقت الحل |
| `next_available_at` | وقت الحل مضافاً إليه مهلة الباقة |
| `refreshing` | `false` |

خلال المهلة يجب ألا يظهر زر بدء الاختبار القديم. بعد انتهاء المهلة فقط يُسمح بتوليد تحدٍّ خاص جديد.

### الدليل البرمجي

- `src/components/DailyQuizCard.tsx` — تحميل slot، حالة الانتظار، وبدء التوليد.
- `src/lib/db.ts` — تحويل استجابة slot وحساب `secondsUntilRefresh`.
- `src/components/QuizResolver.tsx` — الحفظ ثم إكمال التحدي اليومي.
- `supabase/migrations/20260808_repair_daily_completion_and_restore_xp.sql` — RPC `submit_user_daily_quiz_attempt`.

### الإجراء المطلوب

تطبيق الهجرة الأخيرة. إذا كان المستخدم حل الاختبار قبل تطبيقها، فإن جزء reconciliation داخل الهجرة يحاول مطابقة `completions.quiz_id` مع `quiz_payload->>'id'` ثم يمسح payload القديم ويبدأ المهلة.

## BUG-003 — التحدي اليومي يظهر في الاختبارات العامة

### الوصف

التصميم الجديد يمنع استخدام `createQuiz` للتحديات اليومية، ويحفظ payload داخل slot خاص بالمستخدم. لكن الاختبارات القديمة التي أُنشئت قبل التعديل قد تبقى في جدول `quizzes` حتى تُنظف بهجرة مناسبة.

### التحقق

ابحث في `quizzes` عن السجلات التي تحمل عنواناً يومياً أو معرفات يومية. لا ينبغي أن تُنشأ سجلات جديدة أثناء توليد التحدي اليومي.

## BUG-004 — الكوبون لا يبقى بعد Refresh ولا يزيد الاستخدام

### الوصف

الواجهة تحتاج إلى RPC `redeem_coupon_for_user` حتى تحفظ استرداد الكوبون وتحديث الخطة وزيادة `used_count` وتسجيل `coupon_usages` داخل معاملة واحدة. إذا لم تُطبق الهجرة، قد يظهر نجاح محلي أو قد يحدث تحديث جزئي فقط.

### خطوات إعادة الإنتاج

1. استخدم كوبوناً صالحاً على حساب اختبار.
2. تحقق من رسالة النجاح.
3. افحص `users` و`coupon_usages` و`coupon_codes.used_count`.
4. نفّذ Refresh ثم افحص الخطة مرة أخرى.

### النتيجة المتوقعة

يجب أن يكون سجل الاستخدام موجوداً، وأن تزيد قيمة `used_count` مرة واحدة، وتظل الخطة وتاريخ التجديد محفوظين بعد Refresh.

### الدليل والإجراء المطلوب

- `src/pages/UserProfile.tsx`.
- `src/lib/db.ts`.
- `supabase/migrations/20260808_atomic_coupon_redemption.sql`.

يجب تطبيق الهجرة، ثم اختبار كوبون جديد أو حساب جديد لأن حماية الاستخدام قد تمنع إعادة استعمال كوبون سبق تسجيله.

## BUG-005 — مراقبة AI في لوحة الأدمن

### الوصف

القراءة المباشرة من سجل مراقبة AI قد تُحجب بواسطة RLS. المسار المصحح يستخدم RPC يتحقق من `users.is_admin`.

### خطوات التحقق

1. سجّل الدخول بحساب أدمن حقيقي.
2. افتح لوحة الأدمن ثم تبويب مراقبة AI.
3. افحص استدعاء `get_ai_performance_logs` في Network أو Supabase Logs.
4. اختبر بحساب عادي للتأكد من رفض الوصول.

### الدليل والإجراء المطلوب

- `src/lib/db.ts`.
- مكوّن لوحة الأدمن.
- `supabase/migrations/20260808_ai_monitoring_admin_rpc.sql`.

تطبيق الهجرة ضروري، كما يجب أن يكون `users.is_admin = true` للحساب الإداري. لا ينبغي إظهار مراقبة AI في صفحة الاختبارات العامة.

## BUG-006 — عرض جداول Cosmo

تم تعديل `AIChat.tsx` لتحليل صف الفواصل خليةً خلية ودعم المحاذاة وRTL. يجب إجراء اختبار بصري بعد النشر باستخدام طلب مثل: «اعمل جدول مقارنة بين BMW وMercedes». النتيجة الصحيحة هي جدول منسق، لا نصاً خاماً.

## BUG-007 — فرق بيئة النشر عن قاعدة البيانات

### الوصف

GitHub Actions يبني وينشر الواجهة، لكنه لا يطبق ملفات `supabase/migrations/*.sql` تلقائياً ما لم توجد خطوة نشر صريحة لذلك. لذلك يمكن أن يكون الكود المنشور أحدث من دوال قاعدة البيانات الفعلية.

### إجراء تحقق إلزامي

قبل اعتبار أي إصلاح مكتملاً، يجب تنفيذ الهجرة ذات الصلة في Supabase SQL Editor ثم اختبار العملية من الواجهة ومن قاعدة البيانات. لا يكفي نجاح `npm run build`.

## ترتيب الإصلاح المقترح

| الأولوية | الإجراء |
|---|---|
| 1 | تطبيق `20260808_repair_daily_completion_and_restore_xp.sql` والتحقق من XP والـdaily slot لحساب المستخدم المتأثر. |
| 2 | تطبيق `20260808_atomic_coupon_redemption.sql` والتحقق من الجداول الثلاثة للكوبون. |
| 3 | تطبيق `20260808_ai_monitoring_admin_rpc.sql` والتحقق من `is_admin` ونتيجة RPC. |
| 4 | مراجعة GitHub Actions والتأكد من أن الموقع يخدم آخر commit. |
| 5 | إجراء اختبار يدوي موثق لكل مسار، مع تسجيل نتيجة قاعدة البيانات لا رسالة الواجهة فقط. |

## References

1. [QuizResolver.tsx](src/components/QuizResolver.tsx) — مسار حل الاختبار وحفظ النتيجة.
2. [db.ts](src/lib/db.ts) — استدعاءات Supabase وRPC.
3. [DailyQuizCard.tsx](src/components/DailyQuizCard.tsx) — منطق التحدي اليومي والعداد.
4. [UserProfile.tsx](src/pages/UserProfile.tsx) — استرداد الكوبون وبيانات الملف.
5. [Supabase migrations](supabase/migrations/) — تعريفات الجداول والدوال والإصلاحات.
